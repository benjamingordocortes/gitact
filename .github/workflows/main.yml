name: Test

on:
  workflow_dispatch:
  push:
    branches:
    - devs
  pull_request:
      branches:
        - prod
jobs:
  build:
    runs-on: ubuntu-latest
    env:
      BRANCH_NAME: ${{ github.event_name == 'push' && contains(github.event_path, 'refs/heads/') && github.ref }}
    steps:
    - name: rama
      run: |
        echo "Nombre de la rama: $BRANCH_NAME"
    - name: rama
      run: |
        echo "Nombre de la rama: $BRANCH_NAME"


#jobs: 
#  build:
#    runs-on: ubuntu-latest
#    environment:#
#      name: ${{ github.ref }}
#    steps:
#     -
#        name: Checkout
#        uses: actions/checkout@v4
#      -
#        name: Login to Docker Hub
#        uses: docker/login-action@v3
#        with:
#          username: ${{ secrets.DOCKERHUB_USERNAME }}
#          password: ${{ secrets.DOCKERHUB_TOKEN }}
#      -
#        name: Set up Docker Buildx
#        uses: docker/setup-buildx-action@v3
#      - 
#        name: Build and push
#        uses: docker/build-push-action@v5
#        env: 
#          ENTORNO_GIT: ${{ vars.ENTORNO }}
#        with:
#          context: .
#          push: true
#          tags: ${{ secrets.DOCKERHUB_USERNAME }}/${{ vars.ENTORNO }}:latest
#          build-args: ENTORNO_GIT=${{ vars.ENTORNO }}"""

